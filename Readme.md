# JavaScript Promise Assignment

This project implements a simple arrow function that divides two numbers and returns a Promise. It directly handles successful divisions and division-by-zero errors.

## How to Check
1. Open index.html in a browser.
2. Open Console using F12 to see the outputs.